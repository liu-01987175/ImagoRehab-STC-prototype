name: stc_prototype
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  # camera for the video tracking
  camera: ^0.10.0

  # ML Kit’s Pose Detection (MediaPipe under the hood)
  google_mlkit_pose_detection: ^0.3.0

  # The following adds the Cupertino Icons font to your application.
  cupertino_icons: ^1.0.8

  # HTTP client for your backend calls
  http: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
